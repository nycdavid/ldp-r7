<%= form_with(model: anki_deck) do |form| %>
  <% if anki_deck.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(anki_deck.errors.count, "error") %> prohibited this anki_deck from being saved:</h2>

      <ul>
        <% anki_deck.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
