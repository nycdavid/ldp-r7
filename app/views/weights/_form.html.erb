<%= form_with(model: weight) do |form| %>
  <% if weight.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(weight.errors.count, "error") %> prohibited this weight from being saved:</h2>

      <ul>
        <% weight.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label(:measurement, class: "form-label") %>
    <%= form.text_field(:measurement, class: "form-control") %>
  </div>
  <div>
    <%= form.label(:user, class: "form-label") %>
    <%= form.select(:user_id, User.all.map { |u| [u.name, u.id] }, class: "form-control") %>
  </div>
  <div>
    <%= form.label(:notes, class: "form-label") %>
    <%= form.text_area(:notes, class: "form-control") %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
